# Radek Alza

> Description site for black-eye.cz

### Quick start
1. `git clone`
2. `npm install`
3. `npm start`

In your browser, go to `localhost:3000` and magic happen.

### Production build
`npm run build`
